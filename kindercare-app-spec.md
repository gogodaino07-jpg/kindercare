# kindercare 앱 기획서

## 한 줄 소개
유치원/어린이집 가정통신문·일정 파일을 업로드하면, AI가 우리 아이 나이에 맞는 내용만 추출해서 캘린더와 알림으로 정리해주는 앱

## 기술 스택
- Expo (React Native) + TypeScript
- 캘린더는 앱 자체 내장 캘린더 사용 (폰 기본 캘린더 연동 안 함)
- 가정통신문 내용 분석은 Anthropic Claude API 사용 예정

## 데이터 구조

### 가족 그룹 (Family Group)
- 키(key) 1개 = 가족 그룹 1개
- 소유자(owner): 최초 본인인증 후 "신규 생성"한 사람 — 유일하게 다른 구성원을 내보낼 수 있음
- 구성원(member): "키 입력"으로 합류한 사람들 — 본인만 그룹에서 나갈 수 있음
- 키 재발급: 본인인증 한번 더 거치면 새 키 발급 가능 (기존 키는 무효화)

### 아이 프로필
- 이름 (선택 입력)
- 나이 (필수, 3세~7세 중 선택) — AI가 문서 내용을 필터링하는 핵심 기준
- 반 이름 (선택 입력, 표시 전용, 로직에는 영향 없음)
- 여러 명 등록 가능 (형제자매 지원)

### 일정 (Event)
- 날짜 (같은 날짜에 여러 일정 묶여서 표시됨)
- 제목
- 준비물/메모
- 소속 아이 프로필 ID

**날짜 표시 형식**: 앱 전체(홈 화면, AI 확인 화면, 지난 일정 화면 등)에서 날짜 뱃지는 "M/D(요일)" 형식으로 통일 (예: "7/20(월)")

(참고: 날씨는 일정 데이터에 저장하지 않고, 홈 화면에서 외부 날씨 API를 그때그때 조회해서 상단 주간 날씨 줄에 표시)

## 디자인 톤 (앱 전체 배경)
- 배경색: 옅은 하늘색(#EAF4FB), 흰색 단색 배경 대신 사용
- 화면 상단에 아주 옅은 흰색 구름 모양을 살짝 겹쳐서 "하늘에 구름이 살짝 낀 느낌" 연출 (불투명도 낮게, 콘텐츠 가독성 방해 안 하는 선에서)
- 일정 카드 등 콘텐츠 요소는 흰색 배경 + 옅은 그림자로, 하늘 위에 떠 있는 듯한 느낌
- 홈 화면뿐 아니라 캘린더, 설정 등 앱 전체 화면에 동일하게 적용해 톤 통일

## 화면 구성

### 0. 온보딩 (앱 최초 실행 시)
- 본인인증(전화번호) → "신규 생성" / "키 입력" 중 선택
- **신규 생성**: 키 발급 + "이 키는 꼭 저장해두세요" 안내 → 바로 아이 프로필 설정 화면으로
- **키 입력**: 키 입력 후 인증 → 기존 그룹 데이터 그대로 불러와서 홈 화면으로 (이미 등록된 아이 있으면 바로 보임)

### 0-1. 구성원 관리 (설정 메뉴 안)
- 그룹에 속한 구성원 목록 표시
- 소유자 화면: 본인 제외 각 구성원 옆에 "내보내기" 버튼
- 구성원(비소유자) 화면: 본인 항목에만 "그룹 나가기" 버튼
- 설정 > "키 재발급" 메뉴: 본인인증 다시 거치면 새 키 발급 (기존 키 무효화)

### 1. 아이 프로필 설정 (최초 1회, 이후 "+ 아이 추가"로 계속 추가 가능)
- 사진(선택), 이름(선택), 나이(필수 드롭다운), 반 이름(선택) 입력
- 저장 버튼
- **필수값 검증**: 나이를 선택하지 않고 저장을 누르면 에러 메시지 표시 (저장 안 됨)
- **사진 등록 흐름**: 사진 자리 탭 → 카메라/갤러리 선택 → 갤러리에서 고른 사진은 크롭 화면으로 이동
  - 크롭 화면: 원형 가이드 안에 사진을 드래그로 위치 조정, 핀치 인/아웃(손가락 두 개로 확대·축소)으로 크기 조정
  - "다시 선택" / "적용" 버튼으로 마무리

### 2. 홈 화면
- 상단: 현재 선택된 아이 (이름·나이·반) 표시, 눌러서 다른 아이로 전환
- 상단 캘린더 아이콘: 눌러서 9번 캘린더 화면으로 이동
- **주간 날씨**: 오늘부터 7일치 날씨(요일, 아이콘, 기온)를 가로 스크롤로 보여주는 줄 (오늘/내일은 강조 표시)
- 다가오는 일정 목록 (날짜 가까운 순)
  - "내일"인 일정은 빨간색으로 강조, 스크롤해도 상단에 고정(sticky)
  - 나머지 일정은 그 아래 스크롤 영역에 표시
  - **일정 카드 탭 시 칠판 팝업**: 흰 배경 카드 없이 나무 프레임 칠판 자체가 화면에 바로 뜨는 팝업. 색감은 눈에 편안하도록 옅은 톤 사용 (프레임: 옅은 탄색 #C9A87C, 칠판: 세이지그린 #5C7A6E). 프레임 상단 안쪽에 수정(연필)·닫기(X) 아이콘 배치. 칠판 안에 날짜·제목·준비물을 손글씨 스타일 폰트(Poor Story)로 표시. 수정 누르면 그 자리에서 준비물 편집 후 저장하면 칠판으로 복귀. 이 팝업은 캘린더 화면(9번)에서도 동일하게 재사용
- 하단 고정 버튼: "가정통신문 업로드"

### 3. 가정통신문 업로드
- 사진 찍기 / 갤러리 / 파일에서 가져오기 — 3개 정사각형 버튼, 한 줄에 나란히 배치
- 최대 5장까지 업로드 가능
- 사진은 정사각형 썸네일로, PDF 등 파일은 파일명이 보이는 길쭉한 카드로 구분 표시
- "AI로 내용 분석하기" 버튼
- **중복 업로드 감지**: 이미 등록된 일정과 날짜·제목이 비슷하면 "비슷한 일정이 이미 있어요" 경고를 띄우고, 계속 진행할지 사용자가 결정

### 4. AI 확인·수정 (핵심 화면)
- AI가 추출한 일정을 **날짜별로 그룹핑**해서 카드로 표시
  - 카드 상단에 날짜를 메인으로 표시 (임박한 날짜는 빨간 뱃지)
  - 같은 날짜에 일정이 여러 건이면 카드 안에 리스트로 나열
  - 일정 2건 이상인 날짜 그룹은 접기/펼치기 가능 (화살표 아이콘)
  - 각 날짜 카드 안에 "+ 이 날짜에 일정 추가" 버튼 (AI가 놓친 일정 수동 추가용)
- **일정표 우선 정리 규칙**: 유치원 문서에 요일별 일정표가 있으면, 그 표의 항목을 일정 하나의 기준 주제로 삼아 카드 제목으로 사용. 같은 날짜의 본문 설명은 별도 일정으로 쪼개지 않고, 꼭 필요한 정보(준비물, 배부물 등)만 그 카드의 태그·메모에 보충
- 각 일정: 제목 앞에 주제와 어울리는 아이콘 표시 (예: 도서=책, 급식=그릇, 물놀이=물고기). 제목/준비물 내용 수정 가능, 삭제(휴지통) 가능
- **준비물 표시**: 날짜 뱃지(회색 알약)와 구분되도록, 준비물은 체크 아이콘이 있는 사각 모서리 태그로, 참고 정보는 정보 아이콘 태그로 별도 스타일 적용
- **확인 필요 뱃지**: 나이 판단이 애매한 항목(예: 문서에 연령 표시가 없어 공통 일정으로 추정한 경우) 옆에 노란색 "확인 필요" 뱃지 표시 + 카드 하단에 짧은 판단 이유 문구(전구 아이콘 포함)
- **AI 추출 조건**: 문서에 3세/4세/5세 등 여러 연령 내용이 섞여 있으면, 등록된 형제자매 각각의 나이에 해당하는 내용 + "공통/전체 공지"를 한 번의 업로드로 자동 분배 (아이마다 따로 업로드할 필요 없음)
- **부분 인식/실패 처리**: AI가 읽은 부분까지는 그대로 카드로 보여주고, 놓친 내용은 "+ 이 날짜에 일정 추가" 버튼으로 사용자가 직접 수기 입력
- **화면 스크롤 구조**: 제목 포함 전체가 하나의 스크롤로 자연스럽게 움직이고, "캘린더에 저장" 버튼만 하단 고정 (홈 화면과 동일한 방식)
- "캘린더에 저장" 버튼

### 5. 저장 완료
- 체크 아이콘 + "N건이 캘린더에 등록됐어요" 메시지
- "일정 전날 저녁에 준비물을 알려드릴게요" 안내 문구
- "홈으로 돌아가기" 버튼

### 6. 아이 전환/관리 (홈 화면 상단 이름 눌렀을 때 뜨는 바텀시트)
- 등록된 아이 목록을 카드로 나열 (아바타, 이름, 나이·반 이름)
- 지금 선택된 아이는 배경색 강조 + 체크 아이콘으로 표시
- 다른 아이 카드를 탭하면 바로 그 아이 기준으로 홈 화면 전환
- 각 카드 오른쪽에 연필(수정) 아이콘 — 눌러서 그 아이의 프로필 설정 화면(나이/반 이름)으로 이동
- 맨 아래 "+ 아이 추가" 버튼 — 눌러서 신규 아이 프로필 설정 화면으로 이동

### 7. 알림 설정
- "알림 받기" 토글: 끄면 아래 시간 관련 항목 전체가 화면에서 사라짐(숨김 처리, 흐리게 처리 아님)
- "전날 알림 시간": 일정 전날 이 시간에 알림 (기본값 오전 06:00 등 임의 기본값, 사용자가 자유롭게 변경)
- "당일 아침 알림 추가" 토글: 켜면 그 아래에 "당일 알림 시간" 항목이 나타남
- **시간 선택 방식**: 숫자 입력이 아니라 스크롤 휠 피커
  - 오전/오후 · 시(1~12) · 분(5분 단위: 00, 05, 10 ... 55) 3개 열을 각각 스크롤해서 선택
  - 시·분 휠은 끝까지 스크롤하면 처음으로 자연스럽게 이어지는 무한 루프 방식 (오전/오후는 루프 없음)
  - 최종 표시 형식: "오전 09:00" 처럼 오전/오후 + 2자리 시:분
- "저장" 버튼
- **알림 발송 방식**: 서버 푸시가 아닌 **로컬 알림(local notification)**. 일정 저장 시 Expo Notifications로 폰에 알림을 미리 예약해두고, 설정된 시간에 다른 앱과 동일하게 기기 알림창(노티바)에 표시됨. 서버·인터넷 연결 없이도 동작

### 8. 지난 일정 모아보기
- 홈 화면에서 진입 (상단 아이콘 등으로 이동)
- 지난 일정을 월별로 그룹핑해서 최신순으로 표시 (예: "2026년 7월" → "2026년 6월" ...)
- 각 일정 카드: 날짜/제목 표시 + **삭제(휴지통) 버튼** — 원치 않는 지난 기록은 지울 수 있음
- 카드 탭 시 동일한 칠판 팝업으로 상세(준비물 등) 확인 가능하나, **수정 버튼 없이 닫기(X)만 있는 읽기 전용 버전**
- 수정 기능은 없음 (지난 일정은 확인·삭제만 가능)

### 광고 배너
- 홈 화면 상단(주간 날씨 아래, 일정 목록 위)에 배너 자리 확보
- 쿠팡 파트너스 배너 코드 삽입 예정. 배너 하단에 공정위 고지 문구 "이 포스팅은 쿠팡 파트너스 활동의 일환으로, 이에 따른 일정액의 수수료를 제공받습니다" 필수 표기
- 구현 시 WebView로 배너 HTML 렌더링 필요

### 9. 캘린더 (월 단위 그리드)
- 홈 화면 상단 캘린더 아이콘으로 진입
- 월 이동 화살표(◀▶) + "2026년 7월" 형식 표시
- 요일 헤더(일~토) + 날짜 그리드, 오늘은 강조 표시
- **일정 있는 날짜에 색상 점 표시** (3가지로 구분):
  - 남색: 유치원 공지 (가정통신문에서 AI가 추출·확정한 일정)
  - 주황색: 확인 필요 (AI 확인 화면에서 "확인 필요" 뱃지가 붙었던 항목, 아직 미해결)
  - 핑크색: 직접 추가한 개인 일정 (9-1번 참고)
  - 화면 하단에 3색 범례 표시
- 날짜를 탭하면 그 아래에 해당 날짜의 일정 목록이 나타나고, 일정을 다시 탭하면 칠판 모달(2번 홈 화면과 동일)로 상세보기·수정

### 9-1. 일정 수동 추가
- 캘린더 화면의 "+ 일정 추가" 버튼으로 진입
- 입력 항목: 날짜(필수), 제목(필수), 준비물/메모(선택)
- 가정통신문 업로드 없이도 병원 예약 등 개인 일정을 직접 등록 가능
- 저장하면 캘린더에 "직접 추가"(핑크 점)로 표시됨

### 홈 화면 빈 상태 (일정이 하나도 없을 때)
- 상단 아이 선택 영역은 평소와 동일하게 유지
- 가운데: 여자아이·남자아이 캐릭터가 나란히 서있는 귀여운 일러스트 (책 소품 포함)
- 문구: "아직 등록된 게 없어요!" + "가정통신문만 올려주시면 제가 센스있게 정리해드릴게요!" 같은 발랄한 톤
- 하단 "가정통신문 업로드" 버튼은 평소 위치 그대로 유지

### 10. 설정 메인 화면
- **아이**: 아이 프로필 관리, 알림 설정
- **가족 계정**: 구성원 관리, 키 재발급
- **화면 및 보안**: 테마(라이트/다크), 앱 잠금, **글씨체(10-1)**, **칠판 테마 색상(10-2)**
- **기타**: 지난 일정 모아보기

### 10-1. 글씨체 설정
- 칠판 팝업(2, 8, 9번 화면)에서 사용할 글씨체를 사용자가 선택
- 총 11종, 라디오 버튼 형태로 각 글씨체 미리보기 텍스트와 함께 표시
- **기본값: 기본 돋움체** (손글씨 아님, 깔끔하게 읽고 싶은 사용자를 위한 옵션)
- 선택 가능 목록: 기본 돋움체(기본값) / 시스템 폰트(기기 OS 기본 폰트 그대로 따름) / Gaegu / Gamja Flower / Hi Melody / Poor Story / Jua / Dongle / Yeon Sung / Sunflower / East Sea Dokdo

### 10-2. 칠판 테마 색상
- 칠판 팝업의 나무 프레임·배경 색 조합을 12가지 중에서 선택
- 원형 색상 스와치로 표시, 탭하면 바로 적용
- 목록: ① 기본(#C9A87C + #5C7A6E, 기본값) ② 딥우드(#8A5A2B + #2C4A3E) ③ 핑크로즈(#E8B4BC + #6B4C5C) ④ 레몬옐로우(#F0D080 + #33485C) ⑤ 민트(#B8D8C8 + #3A3A3A) ⑥ 코랄(#F0B090 + #4A3B35) ⑦ 그레이 다크모드(#9C9C9C + #1E1E1E) ⑧ 라벤더(#D9C9EC + #4A3B5C) ⑨ 스카이블루(#B8D4E8 + #2A3F52) ⑩ 피치(#F5C9A8 + #5C4030) ⑪ 올리브(#C9C29A + #3A3F2E) ⑫ 로즈골드(#E8C4B8 + #5C2E3A)

## 향후 확장 아이디어 (지금은 구현 안 함)
- 습관 스티커판 기능 (완료 시 부모에게 푸시 알림, 위시리스트 연동)
- 배포 (앱스토어/플레이스토어)
