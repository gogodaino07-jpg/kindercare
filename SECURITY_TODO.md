# 마켓 출시 전 점검 — 남은 항목

이 문서는 2026-08-25 전체 코드 리뷰에서 나온 항목 중, 코드 수정만으로 끝나지 않아
**사람이 직접 확인/조치해야 하는 것들**만 정리한 것입니다. 코드로 고칠 수 있는 항목은
이미 커밋 `3400acc`에 반영했습니다.

---

## ✅ 이미 고친 것 (커밋 `3400acc`)

- **카카오 로그인 계정 탈취 취약점**: 이메일만 알면 계산 가능했던 결정적(deterministic)
  비밀번호 로그인 방식을 없애고, 카카오 액세스 토큰을 서버(`kakaoSignIn` 함수)에서
  직접 검증한 뒤 Firebase Custom Token을 발급하는 방식으로 교체.
- **AI 스캔 사용량 무제한 우회 위험**: `analyzeNewsletter` 함수에 서버 사이드 월간
  상한(뉴스레터 50회/급식표 15회)을 추가해, 클라이언트 한도 체크를 우회해도 비용이
  무한정 새지 않도록 최종 방어선을 둠.
- **가족 초대 코드 무차별 대입 내성 강화**: 숫자 6자리 → 8자리로 확장 (100만 → 1억 가지).
- **키스토어 비밀번호 노출**: 옛 `PROJECT_STATUS.md`에 평문으로 적혀 있던 걸 삭제.
  (다행히 지금 실제로 쓰는 서명 키는 아니었음. 다만 git 히스토리에는 계속 남아있음 —
  꼭 지우고 싶다면 아래 "선택 항목" 참고.)

---

## 🔴 지금 바로 해야 할 것 — 배포

**위 서버(Cloud Functions) 수정은 코드로만 반영됐고, 아직 실제 서버에 올라가지 않았습니다.**
아래 명령으로 배포해야 실제로 적용됩니다.

```bash
cd functions
npm install          # firebase-admin이 새로 추가됐으니 한 번 더 설치
cd ..
firebase deploy --only functions
```

배포 후 확인할 것:
1. **카카오 로그인으로 실기기 테스트** — 기존에 카카오로 가입했던 계정으로도 정상 로그인되는지
   (배포 즉시 그 계정의 예전 비밀번호는 서버가 자동으로 무작위 값으로 덮어써서, 새 방식으로만
   로그인 가능해집니다. 문제 생기면 Firebase 콘솔 → Functions 로그(`kakaoSignIn`)에서 원인 확인).
2. AI 스캔(가정통신문/급식표) 정상 동작 확인.
3. 이 배포는 서버 쪽 변경이라 앱을 새로 빌드하지 않아도 적용되지만, 클라이언트 코드도
   같이 바뀌었으므로(`GeminiAnalysisService.ts`, `AppDataContext.tsx`, `JoinCodeModal.tsx`)
   **앱도 릴리즈 빌드로 다시 만들어서 스토어에 올려야** 두 변경이 같이 맞물려 작동합니다.

---

## 🟠 콘솔에서 직접 확인해야 하는 것 (코드로는 확인 불가)

### 1. Firestore 보안 규칙
레포에 규칙 파일이 없어(`firebase.json`에 firestore 섹션 자체가 없음) 코드 리뷰로는 확인이
안 됩니다. **Firebase 콘솔 → Firestore Database → 규칙 탭**에서 아래를 확인해주세요:
- `familyInvites/{code}` — 인증 없이도(또는 아무 로그인 사용자나) 읽을 수 있게 되어 있는지.
  의도된 설계이긴 하지만, 초대 코드를 8자리로 늘린 것과 별개로 **쓰기는 반드시 서버(Functions)나
  엄격한 규칙으로만 가능하게** 되어 있는지 확인.
- `users/{email}/members/{memberEmail}` — 아무 로그인 사용자나 임의의 `{email}` 아래에
  쓸 수 있게 열려있지 않은지 (초대 코드 검증 없이 직접 문서를 써서 남의 가족에 끼어드는 것을
  막으려면, 최소한 `request.auth.token.email == memberEmail` 정도는 강제해야 함).
- `users/{email}/serverGuard/*` — 이번에 새로 추가한 서버 전용 카운터 문서라, **클라이언트
  쓰기를 아예 막아두는 게** 안전합니다(`allow write: if false;`).

### 2. 관광공사 API 키 (`EXPO_PUBLIC_TOUR_API_KEY`)
클라이언트 번들에 노출되는 값입니다. 발급받은 콘솔(공공데이터포털 등)에서 일일 호출량
제한이 걸려있는지, 걸려있다면 남용당해도 계정 정지·과금으로 번지지 않는 수준인지 확인.

---

## 🟡 우선순위 낮음 / 여유 있을 때

- **RevenueCat 구독 여부가 클라이언트 신고값 그대로 신뢰됨**: `isSubscribed`를 서버가
  검증하지 않습니다. 이번에 추가한 서버 상한은 "프리미엄 최대치"를 기준으로 관대하게 잡아뒀기
  때문에 당장 급한 문제는 아니지만, 나중에 여유 되면 RevenueCat 웹훅을 Cloud Function으로
  받아서 Firestore에 구독 상태를 서버가 직접 기록하는 방식으로 개선하면 더 안전합니다.
- **`expo-camera`/`expo-contacts` 권한 안내 문구**: 다른 권한(위치·사진첩·생체인증)은
  전부 한국어 커스텀 문구가 있는데 이 둘만 `app.json` plugins 설정이 없어서 기본 영문
  문구가 뜰 수 있음. `app.json`의 `plugins`에 `expo-camera`/`expo-contacts` 항목을
  추가하고 한국어 설명을 넣으면 됨.
- **`assistant_rules.md`**: `CLAUDE.md`로 대체된 옛 인수인계 메모라 내용이 중복/충돌함.
  삭제하거나 CLAUDE.md로 통합 권장 (민감 정보는 없어서 긴급하지 않음).

### 선택 항목 — git 히스토리 정리
`PROJECT_STATUS.md`는 지웠지만 예전 커밋 기록에는 키스토어 비밀번호(`kindercare123`,
현재 안 쓰는 키)가 계속 남아있습니다. 실제 위험은 낮지만(이미 폐기된 키 + 키 파일 자체는
유출 안 됨) 깨끗하게 지우고 싶다면 `git filter-repo`나 BFG Repo-Cleaner로 히스토리를
재작성해야 합니다 — 이건 되돌리기 어렵고 원격 저장소도 강제 push해야 해서, 원하시면
별도로 말씀해주세요.

---

## 🎨 디자인 — 보류 중

다크모드 화면별 점검은 사용자 요청으로 잠시 중단. 준비되면 말씀해주시면 릴리즈 빌드
설치 후 화면별로 스크린샷 찍어가며 확인하겠습니다. (참고: `constants/theme.ts`의
다크모드 팔레트를 안 쓰고 색을 하드코딩한 파일이 61개 발견됨 — 온보딩류처럼 의도적으로
항상 밝은 화면도 섞여있어서 실제 확인이 필요함.)
