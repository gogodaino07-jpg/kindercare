# 마켓 출시 전 점검 — 남은 항목

이 문서는 2026-08-25 전체 코드 리뷰에서 나온 항목 중, 코드 수정만으로 끝나지 않아
**사람이 직접 확인/조치해야 하는 것들**만 정리한 것입니다. 코드로 고칠 수 있는 항목은
이미 커밋 `3400acc`에 반영했습니다.

---

## ✅ 이미 고친 것 (커밋 `3400acc`, `f9fab62`, `60c27ae`, `28ab0d0`, `fbc50a0`)

- **카카오 로그인 계정 탈취 취약점**: 이메일만 알면 계산 가능했던 결정적(deterministic)
  비밀번호 로그인 방식을 없애고, 카카오 액세스 토큰을 서버(`kakaoSignIn` 함수)에서
  직접 검증한 뒤 Firebase Custom Token을 발급하는 방식으로 교체. Cloud Functions 배포
  및 IAM 권한(Service Account Token Creator) 부여, 실기기 로그인까지 확인 완료.
- **AI 스캔 사용량 무제한 우회 위험**: `analyzeNewsletter` 함수에 서버 사이드 월간
  상한(뉴스레터 50회/급식표 15회)을 추가해, 클라이언트 한도 체크를 우회해도 비용이
  무한정 새지 않도록 최종 방어선을 둠. 배포 완료.
- **가족 초대 코드 무차별 대입 내성 강화**: 숫자 6자리 → 8자리로 확장 (100만 → 1억 가지).
- **키스토어 비밀번호 노출**: 옛 `PROJECT_STATUS.md`에 평문으로 적혀 있던 걸 삭제.
- **Firestore 보안 규칙 감사 및 배포 완료**: 실제 콘솔에 배포돼 있던 규칙을 전부 가져와
  코드와 대조 검증. 가족 공유 구조(초대 코드 검증, 구성원 읽기/쓰기 분리)는 이미 탄탄하게
  짜여 있었음을 확인. 두 가지만 보강해서 배포함(`firestore.rules`, 커밋 `fbc50a0`):
  - `serverGuard`(AI 사용량 서버 전용 카운터) 컬렉션이 `users/{email}/{allPaths=**}`에
    걸려 본인이 직접 값을 고쳐서 한도를 우회할 수 있었음 → 완전 차단으로 수정.
  - 가족 구성원이 자기 `members` 문서를 직접 조작해 `canEdit: true`를 심어 편집 권한을
    자가 승격할 수 있었음 → `canEdit`는 `role`이 "엄마"/"아빠"일 때만 true가 되도록
    서버 검증 추가(앱의 기존 EDIT_ROLES 로직과 동일하게 유지, 정상 사용 흐름은 안 바뀜).
  - `--dry-run`으로 컴파일 검증 후 배포, 실기기로 앱 정상 동작(데이터 로딩) 확인함.
- **다크모드 버그 다수 수정**: 설정 12개 화면 배경 고정 버그, 다크 팔레트 gray400/500/600
  반전 누락, 알림센터 팝업 흰 배경, 캘린더 전체(달력/일정추가·수정 모달 등) 다크모드 지원
  추가, 쿠팡 배너 구분선. 전부 실기기 확인 완료 — 자세한 내용은 메모리
  `project_dark_mode_bugs_fixed` 참고.
- **홈 화면 개선**: 일정 없는 빈 화면에도 당겨서 새로고침(날씨 재조회+생일 효과) 추가,
  생일 축하 이펙트를 이모지 확산 방식에서 폭죽이 여러 발 터지는 방식으로 리뉴얼.

---

## 🟠 콘솔에서 직접 확인해야 하는 것 (코드로는 확인 불가)

### 1. 관광공사 API 키 (`EXPO_PUBLIC_TOUR_API_KEY`)
클라이언트 번들에 노출되는 값입니다. 발급받은 콘솔(공공데이터포털 등)에서 일일 호출량
제한이 걸려있는지, 걸려있다면 남용당해도 계정 정지·과금으로 번지지 않는 수준인지 확인.

---

## 🟡 우선순위 낮음 / 여유 있을 때

- **RevenueCat 구독 여부가 클라이언트 신고값 그대로 신뢰됨**: `isSubscribed`를 서버가
  검증하지 않습니다. 이번에 추가한 서버 상한은 "프리미엄 최대치"를 기준으로 관대하게 잡아뒀기
  때문에 당장 급한 문제는 아니지만, 나중에 여유 되면 RevenueCat 웹훅을 Cloud Function으로
  받아서 Firestore에 구독 상태를 서버가 직접 기록하는 방식으로 개선하면 더 안전합니다.
- **`expo-camera`/`expo-contacts` 권한 안내 문구**: 다른 권한(위치·사진첩·생체인증)은
  전부 한국어 커스텀 문구가 있는데 이 둘만 `app.json` plugins 설정이 없어서 기본 영문
  문구가 뜰 수 있음. `app.json`의 `plugins`에 `expo-camera`/`expo-contacts` 항목을
  추가하고 한국어 설명을 넣으면 됨.
- **`assistant_rules.md`**: `CLAUDE.md`로 대체된 옛 인수인계 메모라 내용이 중복/충돌함.
  삭제하거나 CLAUDE.md로 통합 권장 (민감 정보는 없어서 긴급하지 않음).

### 선택 항목 — git 히스토리 정리
`PROJECT_STATUS.md`는 지웠지만 예전 커밋 기록에는 키스토어 비밀번호(`kindercare123`,
현재 안 쓰는 키)가 계속 남아있습니다. 실제 위험은 낮지만(이미 폐기된 키 + 키 파일 자체는
유출 안 됨) 깨끗하게 지우고 싶다면 `git filter-repo`나 BFG Repo-Cleaner로 히스토리를
재작성해야 합니다 — 이건 되돌리기 어렵고 원격 저장소도 강제 push해야 해서, 원하시면
별도로 말씀해주세요.

---

## 🎨 디자인 — 남은 것

다크모드는 설정 전체·캘린더까지 실기기로 확인 완료. 아래는 의도적으로 그대로 둔 것들
(확신 낮은 항목은 실제로 보다가 이상하면 바로 알려주세요):
- 스탬프보드, 온보딩 체인 화면들 — 원래부터 고정 라이트 톤으로 디자인된 것으로 보여 유지.
- 개인정보처리방침 화면 — 법적 문서라 흰 배경/검은 텍스트로 고정.
- `BlackboardModal` — 다크모드 대응 코드가 아예 없는데, 확신이 낮아 일단 보류.
