<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/row_container"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="horizontal"
    android:gravity="center_vertical"
    android:background="@drawable/widget_child_row_bg_unselected"
    android:paddingStart="10dp"
    android:paddingEnd="10dp"
    android:paddingTop="7dp"
    android:paddingBottom="7dp"
    android:layout_marginBottom="6dp">

    <FrameLayout
        android:layout_width="26dp"
        android:layout_height="26dp">

        <ImageView
            android:id="@+id/row_avatar"
            android:layout_width="26dp"
            android:layout_height="26dp"
            android:background="@drawable/widget_child_avatar_circle"
            android:clipToOutline="true"
            android:scaleType="centerCrop"
            android:visibility="gone" />

        <TextView
            android:id="@+id/row_avatar_emoji"
            android:layout_width="26dp"
            android:layout_height="26dp"
            android:background="@drawable/widget_child_avatar_emoji_bg"
            android:gravity="center"
            android:textSize="13sp" />
    </FrameLayout>

    <TextView
        android:id="@+id/row_name"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:layout_marginStart="10dp"
        android:textSize="14sp"
        android:textStyle="bold"
        android:textColor="#1E293B"
        android:maxLines="1"
        android:ellipsize="end" />

    <ImageView
        android:id="@+id/row_check"
        android:layout_width="20dp"
        android:layout_height="20dp"
        android:src="@drawable/widget_child_check_unselected" />
</LinearLayout>
